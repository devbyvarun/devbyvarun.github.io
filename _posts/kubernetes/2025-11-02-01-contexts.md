---
title: "Kubernetes Contexts"
date: 2025-11-02 12:00:00 -0700
categories: [Kubernetes]
tags: [kubernetes, intro]
description: "A context in Kubernetes defines which cluster, user, and namespace kubectl talks to by default."
---

### **List all available contexts**

```bash
kubectl config get-contexts
```

* The current context is marked with an asterisk (`*`).

### **View the current context**

```bash
kubectl config current-context
```

### **Switch to a different context**

```bash
kubectl config use-context <context-name>
```

### **Add or rename a context** (optional)

```bash
kubectl config rename-context <old-name> <new-name>
```
